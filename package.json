{
  "name": "widgetmedizona",
  "version": "1.0.0",
  "description": "Widget Medizona",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "build": "babel src --out-dir dist && terser dist/widget.js -o dist/widget.min.js --compress --mangle"
  },
  "author": "Gerzain",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.25.9",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "terser": "^5.36.0"
  }
}
